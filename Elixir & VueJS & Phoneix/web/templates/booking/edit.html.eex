<h2>Edit booking</h2>

<%= render "form.html", changeset: @changeset,
                        action: booking_path(@conn, :update, @booking) %>

<span><%= link "Back", to: booking_path(@conn, :index) %></span>
